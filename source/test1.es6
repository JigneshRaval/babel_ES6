'use strict';

import * as net from 'test2';

console.log(x);

class Car {
  constructor(name) {
    this.name = name;
  }

  getName() {
    console.log(this.name);
  }

  setName(name) {
    this.name = name;
    console.log(this.name);
  }
}

net.square(10);
